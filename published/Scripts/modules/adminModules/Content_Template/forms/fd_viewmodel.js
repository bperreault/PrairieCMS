define(["Boiler"], function (Boiler) {

    var ViewModel = function (moduleContext) {

        var self = this;        

        self.initialize = function () {
            
        };

        self.setItem = function (item) {
//            self.pkDripItemId(item.pkDripItemId);
//            self.isActive(item.isActive);
//            self.itemName(item.itemName);
//            self.fkContentId(item.fkContentId);
//            self.fkListId(item.fkListId);
//            self.fkTemplateId(item.fkTemplateId);
//            self.Subject(item.Subject);
//            self.HourToRun(item.HourToRun);
        }
      
    };

    return ViewModel;
});

